import { CheckbookStatusHist } from './checkbook-status-hist';

describe('CheckbookStatusHist', () => {
  it('should create an instance', () => {
    expect(new CheckbookStatusHist()).toBeTruthy();
  });
});
