import { AccountStatusHist } from './account-status-hist';

describe('AccountStatusHist', () => {
  it('should create an instance', () => {
    expect(new AccountStatusHist()).toBeTruthy();
  });
});
